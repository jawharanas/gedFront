interface EmployeeDTO {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
  }


  interface ManagerDTO {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
  }
  interface LoginDTO{
    email: string;
    password: string;
  }